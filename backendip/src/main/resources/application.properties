# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/turf_explorer?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Debirupa12}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=${SPRING_SQL_INIT_MODE:never}

# JWT Configuration
jwt.secret=turfExplorerSecretKeyForJWTTokenGenerationAndValidation2024SecureKey
jwt.expiration=86400000

# Auth rate limiting
auth.rate-limit.login.per-ip=${AUTH_RATE_LIMIT_LOGIN_PER_IP:20}
auth.rate-limit.login.per-email=${AUTH_RATE_LIMIT_LOGIN_PER_EMAIL:10}
auth.rate-limit.login.window-seconds=${AUTH_RATE_LIMIT_LOGIN_WINDOW_SECONDS:60}
auth.rate-limit.register.per-ip=${AUTH_RATE_LIMIT_REGISTER_PER_IP:10}
auth.rate-limit.register.per-email=${AUTH_RATE_LIMIT_REGISTER_PER_EMAIL:3}
auth.rate-limit.register.window-seconds=${AUTH_RATE_LIMIT_REGISTER_WINDOW_SECONDS:900}
auth.rate-limit.verify-otp.per-ip=${AUTH_RATE_LIMIT_VERIFY_OTP_PER_IP:30}
auth.rate-limit.verify-otp.per-email=${AUTH_RATE_LIMIT_VERIFY_OTP_PER_EMAIL:6}
auth.rate-limit.verify-otp.window-seconds=${AUTH_RATE_LIMIT_VERIFY_OTP_WINDOW_SECONDS:600}
auth.rate-limit.max-entries=${AUTH_RATE_LIMIT_MAX_ENTRIES:20000}

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.turfexplorer=DEBUG
groq.api.key=${GROQ_API_KEY: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}

# bKash Tokenized Checkout (Sandbox)
bkash.app.key=4f6o0cjiki2rfm34kfdadl1eqq
bkash.app.secret=2is7hdktrekvrbljjh44ll3d9l1dtjo4pasmjvs5vl5qr3fug4b
bkash.username=sandboxTokenizedUser02
bkash.password=sandboxTokenizedUser02@12345
bkash.base.url=https://tokenized.sandbox.bka.sh/v1.2.0-beta/tokenized

# Frontend URL used for payment redirects
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# Email SMTP Configuration (Gmail/Google Workspace App Password)
spring.mail.host=${EMAIL_HOST:smtp.gmail.com}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=u2204070@student.cuet.ac.bd
spring.mail.password=pjsx vzxm fsel wilu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=${EMAIL_SSL_TRUST:smtp.gmail.com}
spring.mail.properties.mail.smtp.ssl.checkserveridentity=false
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# OTP Email Sender
app.email.sender=u2204070@student.cuet.ac.bd

# Abandoned booking cleanup
cleanup.abandoned-booking.fixed-delay-ms=${CLEANUP_ABANDONED_BOOKING_FIXED_DELAY_MS:300000}
cleanup.abandoned-booking.cutoff-minutes=${CLEANUP_ABANDONED_BOOKING_CUTOFF_MINUTES:15}